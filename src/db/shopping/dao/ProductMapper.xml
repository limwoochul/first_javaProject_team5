<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="db.shopping.dao.ProductDAO">
	<resultMap type="CartVO" id="CartMap">
		<id property="ca_num" column="ca_num"/>
		<result property="ca_amount" column="ca_amount"/>
		<result property="ca_me_id" column="ca_me_id"/>
		<result property="ca_pr_num" column="ca_pr_num"/>
		<collection property="product" ofType="ProductVO" column="ca_pr_num" select="selectProduct"/>
	</resultMap>
    
	<select id="selectCategoryList" resultType="CategoryVO">
		select * from category
	</select>
	<select id="selectProductList" resultType="ProductVO">
		select * from product where pr_cg_num = #{cg_num}
	</select>
	
	<select id="selectCart" resultType="CartVO">
		select * from cart where ca_me_id = #{me_id} and ca_pr_num = #{pr.pr_num}
	</select>
	<update id="updateCart">
		update cart set ca_amount = ca_amount + #{pr.pr_inventory} where ca_me_id = #{me_id} and ca_pr_num = #{pr.pr_num}
	</update>
	
	<insert id="insertCart">
		insert into cart(ca_amount, ca_me_id, ca_pr_num)
		values(#{pr.pr_inventory}, #{me_id}, #{pr.pr_num})
	</insert>
	
	<select id="selectProductName" resultType="ProductVO">
		select * from product where pr_name like concat('%', #{pr_name} ,'%')
	</select> 
	
	<select id="selectCartList" resultMap="CartMap">
		select * from cart where ca_me_id = #{me_id}
	</select>
	<select id="selectProduct" resultType="ProductVO">
		select * from product where pr_num = #{pr_num}
	</select>
	
	<update id="updateProductAmount">
		update product set pr_inventory = pr_inventory - #{ca.ca_amount}
		where pr_num = #{ca.ca_pr_num}
	</update>
	
	<delete id="deleteSomeProduct">
		delete from cart where ca_pr_num = #{pr_num} and ca_me_id = #{me_id}
	</delete>
	<delete id="deleteAllProduct">
		delete from cart where ca_me_id = #{me_id}
	</delete>
		
	<insert id="insertBuy">
		insert into buy (bu_amount, bu_pr_num, bu_me_id)
		values( #{ca.ca_amount} , #{ca.ca_pr_num} , #{me_id} )
	</insert>
	
	<insert id="insertProduct" parameterType="db.shopping.model.vo.ProductVO">
        INSERT INTO product (pr_name, pr_price, pr_inventory, pr_cg_num)
        VALUES (#{product.pr_name}, #{product.pr_price}, #{product.pr_inventory}, #{product.pr_cg_num})
    </insert>

    <update id="updateProduct" parameterType="db.shopping.model.vo.ProductVO">
        UPDATE product
        SET pr_name = #{product.pr_name}, pr_price = #{product.pr_price}, pr_inventory = #{product.pr_inventory}, pr_cg_num = #{product.pr_cg_num}
        WHERE pr_num = #{product.pr_num}
    </update>

    <delete id="deleteProduct" parameterType="int">
        DELETE FROM product WHERE pr_num = #{pr_num}
    </delete>

    <select id="selectProductAll" resultType="db.shopping.model.vo.ProductVO">
       SELECT *, category.* 
       FROM product
       JOIN category ON pr_cg_num = cg_num
       ORDER BY pr_num ASC;
    </select>

    <update id="updateProductNumbers" parameterType="int">
        UPDATE product
        SET pr_num = pr_num - 1
        WHERE pr_num > #{pr_num}
    </update>
	
	<select id="checkProduct" resultType="ProductVO">
		select * from product where pr_num = #{pr_num}
	</select>
	
</mapper>